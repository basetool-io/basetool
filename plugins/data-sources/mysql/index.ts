import { DataSourceInfo } from "../types"

const info: DataSourceInfo = {
  id: "mysql",
  name: "MySQL",
  description: "MySQL data source",
  readOnly: false,
  requests: {
    columns: true
  }
};

export default info;

import { DataSourceInfo } from "../types";

const info: DataSourceInfo = {
  id: "stripe",
  name: "Stripe",
  description: "Stripe",
  readOnly: true,
  requests: {
    columns: true
  }
};

export default info;

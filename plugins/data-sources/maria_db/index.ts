import { DataSourceInfo } from "../types"

const info: DataSourceInfo = {
  id: "maria_db",
  name: "Maria DB",
  description: "Maria DB data source",
  readOnly: false,
  requests: {
    columns: true
  }
};

export default info;
